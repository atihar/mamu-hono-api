{
  "private": true,
  "scripts": {
    "dev": "npx prisma generate && tsx watch src/index.ts",
    "start": "npx prisma generate && node src/index.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.7.0",
    "@prisma/client": "^5.9.1",
    "hono": "^3.12.8",
    "prisma": "^5.9.1"
  },
  "devDependencies": {
    "tsx": "^3.12.2"
  }
}
